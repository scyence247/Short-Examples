# Short-Examples
# This short tutorial uses the babynames dataset which contains the names submitted for SSN applications from 1880-2017
# The code loads data, explores data via basic descriptive statistics and generates plots for simple visualizations using ggplot
